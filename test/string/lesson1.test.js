import revertByWorld from '../../code/string/lesson1'

test("revertByWorld: Let's take Algorithm Case", () => {
  expect(revertByWorld("Let's take Algorithm Case")).toBe("s'teL ekat mhtiroglA esaC")
})
